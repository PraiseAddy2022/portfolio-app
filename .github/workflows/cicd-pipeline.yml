on: push

jobs:
   build:
    runs-on: ubuntu-latest
    steps:
       - name: checkout code
         uses: actions/checkout@v6
       - name: build image
         run: docker build -t praiseaddy/portfolioci .
       - name: list image
         run: docker images
         
   test:
    needs: build
    runs-on: ubuntu-latest
    steps: 
       - name: hello
         run: echo hello     
